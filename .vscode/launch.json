{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch CLI",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/misskey-reaction-cli/main.go",
            "args": [
                "-config",
                "${workspaceFolder}/config.yaml"
            ]
        }
    ]
}
